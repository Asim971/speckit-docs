# SpecKit Documentation Mirror

This repository tracks the curated documentation tree exported from the SpecKit workspace.

- Source: `SpecKit/docs/`
- Archive location: `SpecKit/docs/archive/`
- Verification evidence: see `logs/governance/` in the source workspace.

Updates are generated by the Documentation Agent v2 workflow (MCP docker `github-official`).